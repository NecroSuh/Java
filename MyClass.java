package lambda;

// MyInterface를 구현하는 MyClass 정의, MyInterface를 싱속하는 자식클래스 정의, 상속관계 존재.

public class MyClass implements MyInterface {
	@Override
	public void sendMsg(String msg) {
		
	}
}


